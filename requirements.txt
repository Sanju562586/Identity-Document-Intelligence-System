# ============================================================
# Identity Document Intelligence System — Requirements
# ============================================================
# Install with: pip install -r requirements.txt
# For GPU: ensure CUDA-compatible torch is installed first.
# See: https://pytorch.org/get-started/locally/

# ── Core ML ──────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0
peft>=0.10.0
trl>=0.8.6
bitsandbytes>=0.43.0
accelerate>=0.27.0
datasets>=2.18.0
tokenizers>=0.15.0

# ── Vision / OCR ─────────────────────────────────────────────
Pillow>=10.0.0
opencv-python>=4.9.0
albumentations>=1.3.1
easyocr>=1.7.1
pytesseract>=0.3.10
# paddleocr>=2.7.0         # Optional — large install, uncomment if needed
# paddlepaddle>=2.6.0      # Required by paddleocr

# ── Data Generation ───────────────────────────────────────────
Faker>=24.0.0
fpdf2>=2.7.9

# ── Metrics & Evaluation ──────────────────────────────────────
jiwer>=3.0.3
scikit-learn>=1.4.0
scipy>=1.12.0

# ── Data Science ──────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
matplotlib>=3.8.0
seaborn>=0.13.0

# ── Experiment Tracking ───────────────────────────────────────
wandb>=0.16.0

# ── HuggingFace Hub ───────────────────────────────────────────
huggingface_hub>=0.20.0

# ── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.0
tqdm>=4.66.0
pyyaml>=6.0.1
timm>=0.9.16             # ViT-B/16 fallback for forgery head
